spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.open-in-view=false

# Test-only key — never used for real auth, safe to commit. Distinct from
# the production key, which is supplied via the JWT_SECRET_KEY env var.
security.jwt.secret-key=OazXHnqbI9D6qDLbP1HrwA91yETKPjSnd4DEKe5o3NE=
security.jwt.expiration-time=12222200000

spring.rabbitmq.host=localhost
spring.rabbitmq.port=5672
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.amqp.RabbitAutoConfiguration
